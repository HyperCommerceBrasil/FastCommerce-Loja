/**
 * Use this to give the component's or tags's ids.
 * Let's follow the pattern CONTEXT_DESCRIPTION_OF_ID.
 * If there is a list or a large amount where is possible to two or more same
 * ids, you can concantenate a more specific description BUT, using this enum
 * in the start.
 */
export enum IDS_INPUTS {
  CART_ZIP_CODE = 'CART_ZIP_CODE',
}
