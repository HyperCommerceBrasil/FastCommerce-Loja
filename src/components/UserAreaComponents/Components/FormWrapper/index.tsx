import React from 'react';
import { FormWrapper } from './styles';

const FormWrapperContainer: React.FC = ({ children }) => (
  <FormWrapper>{children}</FormWrapper>
);

export default FormWrapperContainer;
